# 🚀 Flask + Docker + PostgreSQL Demo  

This project demonstrates:
- A simple Flask app  
- Containerized with Docker (multi-stage build)  
- PostgreSQL database via Docker Compose  

## Run Locally  

```bash
docker compose up --build
